[project]
name = "text2geoql-dataset"
version = "0.1.0"
description = "Dataset generation for text-to-Overpass QL translation"
requires-python = ">=3.12"
dependencies = [
    "ollama",
    "langchain",
    "langchain-chroma",
    "langchain-ollama",
    "datasets",
    "httpx",
]

[tool.uv]
dev-dependencies = []
